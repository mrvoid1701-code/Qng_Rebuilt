# Figure Plan v1

Type: `note`
ID: `NOTE-GOV-013`
Status: `draft`
Author: `C.D Gabriel`

## Objective

Define the minimum figure set required to make the rebuilt paper readable without relying on verbal compression.

## Figure rule

Every figure must do one of the following:

- explain architecture
- explain dependency order
- summarize validated support
- separate core from open lanes

No figure should imply a theorem stronger than the text supports.

## Figure set

### Figure 1: rebuild architecture

Purpose:

- show the whole paper in one diagram

Content:

`native update -> (C_eff, L_eff) -> GR path + QM path -> bridge -> phenomenology`

Key message:

- one native substrate, two effective channels, multiple derived sectors

### Figure 2: legacy compression vs rebuild correction

Purpose:

- explain why the rebuild was necessary

Content:

- old overloaded `Sigma`
- drifting `tau`
- premature `chi = m/c`
- phenomenology over-carrying theory load
- corrected split and layer discipline

Key message:

- the paper is not just renaming the old project; it is structurally correcting it

### Figure 3: GR recovery ladder

Purpose:

- show the GR-facing derivation order

Content:

`C_eff -> geometry proxy -> Lorentzian signature proxy -> weak-field assembly -> linearized curvature`

Key message:

- GR is the strongest current recovery lane

### Figure 4: QM recovery ladder

Purpose:

- show the QM-facing derivation order

Content:

`(C_eff, phi) -> correlator -> local generator -> mode/spectrum`

Include note:

- continuity closure remains weak/open

Key message:

- the QM lane exists and is strengthening, but not in the same form as GR

### Figure 5: bridge/source-response ladder

Purpose:

- show how the sectors stop being parallel only

Content:

- back-reaction proxy closure
- source-response consistency step

Key message:

- the bridge has first controlled closure structure

### Figure 6: phenomenology descent map

Purpose:

- show downstream branching from the rebuilt core

Content:

- trajectory
- lensing
- rotation
- timing
- cosmology

Key message:

- phenomenology is derived application structure, not theory-defining structure

### Figure 7: status map

Purpose:

- summarize support level at a glance

Content:

- `proxy-supported`
- `candidate`
- `open`

Key message:

- the paper is explicit about support level

### Figure 8: freeze core vs experimental lanes

Purpose:

- prevent silent overreach

Content:

- provisional frozen backbone
- experimental lanes
- open theorem lanes

Key message:

- the manuscript has a stable center and clearly bounded uncertainty

## Minimum viable paper figure subset

If only four figures are used, keep:

1. rebuild architecture
2. GR recovery ladder
3. QM recovery ladder
4. freeze core vs experimental lanes

## Table tie-ins

Recommended table pairings:

- Figure 2 with `legacy compression problems vs rebuild corrections`
- Figure 7 with `status map: proxy-supported / candidate / open`
- Figure 8 with `core freeze vs experimental lanes`

## Production note

Figures should use the rebuilt vocabulary only:

- `C_eff`
- `L_eff`
- GR-facing proxy
- QM-facing proxy
- bridge
- phenomenology

Avoid old shorthand that re-imports compressed legacy meanings.
